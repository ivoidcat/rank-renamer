# Rank Renamer

Win98 风格的批量图片重命名工具，基于 Flask Web 界面。

## 功能

- 缩略图网格预览
- 拖拽调整顺序
- 多种排序方式（名称、修改时间、创建时间、文件大小、扩展名）
- 自定义命名规则（前缀、起始编号、步长、补零位数、后缀）
- 重命名预览窗口
- 输出到独立文件夹，不覆盖原文件

## 使用

```bash
uv run rename.py
```

浏览器自动打开，输入图片文件夹路径即可开始。

## 依赖

- Python >= 3.9
- Flask
- Pillow
